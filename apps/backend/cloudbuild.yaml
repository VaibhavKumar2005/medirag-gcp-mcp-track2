timeout: 1200s

steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.mcp'
      - '-t'
      - '$_IMAGE'
      - '.'

images:
  - '$_IMAGE'

options:
  logging: LEGACY
  machineType: E2_HIGHCPU_8
